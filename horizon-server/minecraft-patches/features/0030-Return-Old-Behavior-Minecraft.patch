From 0000000000000000000000000000000000000000 Mon Sep 17 00:00:00 2001
From: GideonWhite1029 <gideonwhite1029@gmail.com>
Date: Thu, 17 Apr 2025 20:00:51 +0300
Subject: [PATCH] Return Old Behavior Minecraft

Original license: GPLv3
Original project: https://github.com/LeavesMC/Leaves

diff --git a/net/minecraft/network/protocol/PacketUtils.java b/net/minecraft/network/protocol/PacketUtils.java
index 4535858701b2bb232b9d2feb2af6551526232ddc..b175f366568c505bd86f9830f1261831bdfa4893 100644
--- a/net/minecraft/network/protocol/PacketUtils.java
+++ b/net/minecraft/network/protocol/PacketUtils.java
@@ -27,6 +27,10 @@ public class PacketUtils {
                 if (processor.shouldHandleMessage(packet)) {
                     try {
                         packet.handle(processor);
+                        // Horizon start - update suppression crash fix
+                    } catch (dev.gideonwhite1029.horizon.util.UpdateSuppressionException exception) {
+                        dev.gideonwhite1029.horizon.HorizonLogger.LOGGER.info(exception.getMessage());
+                        // Horizon start - update suppression crash fix
                     } catch (Exception var4) {
                         if (var4 instanceof ReportedException reportedException && reportedException.getCause() instanceof OutOfMemoryError) {
                             throw makeReportedException(var4, packet, processor);
diff --git a/net/minecraft/server/MinecraftServer.java b/net/minecraft/server/MinecraftServer.java
index acf3394400c127e4cebe689dfb1ba138b2396703..55a353e964bfb65c7b6c87200967a71afa52e40e 100644
--- a/net/minecraft/server/MinecraftServer.java
+++ b/net/minecraft/server/MinecraftServer.java
@@ -1762,6 +1762,10 @@ public abstract class MinecraftServer extends ReentrantBlockableEventLoop<TickTa
 
             try {
                 serverLevel.tick(hasTimeLeft);
+                // Horizon start - update suppression crash fix
+            } catch (dev.gideonwhite1029.horizon.util.UpdateSuppressionException exception) {
+                dev.gideonwhite1029.horizon.HorizonLogger.LOGGER.info(exception.getMessage());
+                // Horizon start - update suppression crash fix
             } catch (Throwable var7) {
                 CrashReport crashReport = CrashReport.forThrowable(var7, "Exception ticking world");
                 serverLevel.fillReportDetails(crashReport);
diff --git a/net/minecraft/world/level/Level.java b/net/minecraft/world/level/Level.java
index ce6c6bafb55ee40b0c00372f6d2b5545336bacab..dff01dce44d9adfbd47abd4d7e36f888123d9ab6 100644
--- a/net/minecraft/world/level/Level.java
+++ b/net/minecraft/world/level/Level.java
@@ -919,7 +919,13 @@ public abstract class Level implements LevelAccessor, UUIDLookup<Entity>, AutoCl
         this.thread = Thread.currentThread();
         this.biomeManager = new BiomeManager(this, biomeZoomSeed);
         this.isDebug = isDebug;
-        this.neighborUpdater = new CollectingNeighborUpdater(this, maxChainedNeighborUpdates);
+        // Horizon start - instantBlockUpdaterReintroduced
+        if (dev.gideonwhite1029.horizon.HorizonConfig.instantBlockUpdaterReintroduced) {
+            this.neighborUpdater = new net.minecraft.world.level.redstone.InstantNeighborUpdater(this);
+        } else {
+            this.neighborUpdater = new CollectingNeighborUpdater(this, maxChainedNeighborUpdates);
+        }
+        // Horizon end - instantBlockUpdaterReintroduced
         this.registryAccess = registryAccess;
         this.damageSources = new DamageSources(registryAccess);
 
diff --git a/net/minecraft/world/level/block/ChiseledBookShelfBlock.java b/net/minecraft/world/level/block/ChiseledBookShelfBlock.java
index eb315a50a83dc7230d7ad66f4daeb0d632679941..f90bf3b8cebce81cb8e29d4650555a8de0d8593b 100644
--- a/net/minecraft/world/level/block/ChiseledBookShelfBlock.java
+++ b/net/minecraft/world/level/block/ChiseledBookShelfBlock.java
@@ -210,9 +210,13 @@ public class ChiseledBookShelfBlock extends BaseEntityBlock {
         if (level.isClientSide()) {
             return 0;
         } else {
+            if (dev.gideonwhite1029.horizon.HorizonConfig.oldBlockEntityBehaviour) {
+                level.updateNeighbourForOutputSignal(pos, this);
+                return 0;
+            }
             return level.getBlockEntity(pos) instanceof ChiseledBookShelfBlockEntity chiseledBookShelfBlockEntity
-                ? chiseledBookShelfBlockEntity.getLastInteractedSlot() + 1
-                : 0;
+                    ? chiseledBookShelfBlockEntity.getLastInteractedSlot() + 1
+                    : 0;
         }
     }
 }
diff --git a/net/minecraft/world/level/block/LecternBlock.java b/net/minecraft/world/level/block/LecternBlock.java
index 5a9b601b7bf7e80b04ebd8f5c8b7d121031132c7..5df4091fba9d15abf01b8df3fe0e571ef05d475f 100644
--- a/net/minecraft/world/level/block/LecternBlock.java
+++ b/net/minecraft/world/level/block/LecternBlock.java
@@ -199,9 +199,15 @@ public class LecternBlock extends BaseEntityBlock {
 
     @Override
     protected void affectNeighborsAfterRemoval(BlockState state, ServerLevel level, BlockPos pos, boolean movedByPiston) {
+        if (!dev.gideonwhite1029.horizon.HorizonConfig.oldBlockEntityBehaviour) {
+            super.affectNeighborsAfterRemoval(state, level, pos, movedByPiston);
+        }
         if (state.getValue(POWERED)) {
             updateBelow(level, pos, state);
         }
+        if (dev.gideonwhite1029.horizon.HorizonConfig.oldBlockEntityBehaviour) {
+            super.affectNeighborsAfterRemoval(state, level, pos, movedByPiston);
+        }
     }
 
     @Override
diff --git a/net/minecraft/world/level/block/RedStoneWireBlock.java b/net/minecraft/world/level/block/RedStoneWireBlock.java
index 1943a6aad888647953e2d9dbbeedb0bd81c6f9df..a0fa8f132ec4fb003fe0539d42ff48c7c3ffd3fd 100644
--- a/net/minecraft/world/level/block/RedStoneWireBlock.java
+++ b/net/minecraft/world/level/block/RedStoneWireBlock.java
@@ -179,7 +179,7 @@ public class RedStoneWireBlock extends Block {
         RandomSource random
     ) {
         if (direction == Direction.DOWN) {
-            return !this.canSurviveOn(level, neighborPos, neighborState) ? Blocks.AIR.defaultBlockState() : state;
+            return dev.gideonwhite1029.horizon.HorizonConfig.redstoneDontCantOnTrapDoor ? state : !this.canSurviveOn(level, neighborPos, neighborState) ? Blocks.AIR.defaultBlockState() : state; // Horizon - behavior to 1.19
         } else if (direction == Direction.UP) {
             return this.getConnectionState(level, state, pos);
         } else {
@@ -238,7 +238,7 @@ public class RedStoneWireBlock extends Block {
         BlockPos blockPos = pos.relative(direction);
         BlockState blockState = level.getBlockState(blockPos);
         if (nonNormalCubeAbove) {
-            boolean flag = blockState.getBlock() instanceof TrapDoorBlock || this.canSurviveOn(level, blockPos, blockState);
+            boolean flag = (!dev.gideonwhite1029.horizon.HorizonConfig.redstoneDontCantOnTrapDoor && blockState.getBlock() instanceof TrapDoorBlock) || this.canSurviveOn(level, blockPos, blockState); // Horizon - behavior to 1.19
             if (flag && shouldConnectTo(level.getBlockState(blockPos.above()))) {
                 if (blockState.isFaceSturdy(level, blockPos, direction.getOpposite())) {
                     return RedstoneSide.UP;
diff --git a/net/minecraft/world/level/block/SculkSensorBlock.java b/net/minecraft/world/level/block/SculkSensorBlock.java
index f0101e88140f480c1a94f899744991f78c9e3756..7c7c9b36cf87bef860d47e23a22e00b0ed21f124 100644
--- a/net/minecraft/world/level/block/SculkSensorBlock.java
+++ b/net/minecraft/world/level/block/SculkSensorBlock.java
@@ -131,9 +131,15 @@ public class SculkSensorBlock extends BaseEntityBlock implements SimpleWaterlogg
 
     @Override
     protected void affectNeighborsAfterRemoval(BlockState state, ServerLevel level, BlockPos pos, boolean movedByPiston) {
+        if (!dev.gideonwhite1029.horizon.HorizonConfig.oldBlockEntityBehaviour) {
+            super.affectNeighborsAfterRemoval(state, level, pos, movedByPiston);
+        }
         if (getPhase(state) == SculkSensorPhase.ACTIVE) {
             updateNeighbours(level, pos, state);
         }
+        if (dev.gideonwhite1029.horizon.HorizonConfig.oldBlockEntityBehaviour) {
+            super.affectNeighborsAfterRemoval(state, level, pos, movedByPiston);
+        }
     }
 
     @Override
diff --git a/net/minecraft/world/level/block/ShulkerBoxBlock.java b/net/minecraft/world/level/block/ShulkerBoxBlock.java
index 49bac7af90b0a7c490141be6357563447783c6ca..f0bb3b3fe8aa4a8e888f57841e4a46dc48717405 100644
--- a/net/minecraft/world/level/block/ShulkerBoxBlock.java
+++ b/net/minecraft/world/level/block/ShulkerBoxBlock.java
@@ -154,7 +154,13 @@ public class ShulkerBoxBlock extends BaseEntityBlock {
 
     @Override
     protected void affectNeighborsAfterRemoval(BlockState state, ServerLevel level, BlockPos pos, boolean movedByPiston) {
+        if (!dev.gideonwhite1029.horizon.HorizonConfig.oldBlockEntityBehaviour) {
+            super.affectNeighborsAfterRemoval(state, level, pos, movedByPiston);
+        }
         Containers.updateNeighboursAfterDestroy(state, level, pos);
+        if (dev.gideonwhite1029.horizon.HorizonConfig.oldBlockEntityBehaviour) {
+            super.affectNeighborsAfterRemoval(state, level, pos, movedByPiston);
+        }
     }
 
     @Override
@@ -183,7 +189,19 @@ public class ShulkerBoxBlock extends BaseEntityBlock {
 
     @Override
     protected int getAnalogOutputSignal(BlockState blockState, Level level, BlockPos pos) {
-        return AbstractContainerMenu.getRedstoneSignalFromBlockEntity(level.getBlockEntity(pos));
+        // Horizon start - fix update suppression crash
+        try {
+            return dev.gideonwhite1029.horizon.HorizonConfig.cceUpdateSuppression ?
+                    AbstractContainerMenu.getRedstoneSignalFromContainer((net.minecraft.world.Container) level.getBlockEntity(pos)) : // Horizon start
+                    AbstractContainerMenu.getRedstoneSignalFromBlockEntity(level.getBlockEntity(pos));
+        } catch (ClassCastException ex) {
+            if (dev.gideonwhite1029.horizon.HorizonConfig.updateSuppressionCrashFix) {
+                throw new dev.gideonwhite1029.horizon.util.UpdateSuppressionException(pos, this);
+            } else {
+                throw ex;
+            }
+        }
+        // Horizon end - fix update suppression crash
     }
 
     public static Block getBlockByColor(@Nullable DyeColor color) {
diff --git a/net/minecraft/world/level/block/entity/BlockEntity.java b/net/minecraft/world/level/block/entity/BlockEntity.java
index a5ade8268a74738170caf519e7e45e13862bc39d..79590a5bbfea00436c18e164d1b102217c52a34e 100644
--- a/net/minecraft/world/level/block/entity/BlockEntity.java
+++ b/net/minecraft/world/level/block/entity/BlockEntity.java
@@ -65,7 +65,7 @@ public abstract class BlockEntity {
     }
 
     public boolean isValidBlockState(BlockState state) {
-        return this.type.isValid(state);
+        return dev.gideonwhite1029.horizon.HorizonConfig.oldBlockEntityBehaviour || this.type.isValid(state); // Horizon - behaviour 1.21.1
     }
 
     public static BlockPos getPosFromTag(ChunkPos chunkPos, CompoundTag tag) {
diff --git a/net/minecraft/world/level/redstone/NeighborUpdater.java b/net/minecraft/world/level/redstone/NeighborUpdater.java
index 332b33a004ab11150cca0cc2cefc26d0286648f5..1ea0ad1ae47d50a12a171b98e5c5b85de70571ed 100644
--- a/net/minecraft/world/level/redstone/NeighborUpdater.java
+++ b/net/minecraft/world/level/redstone/NeighborUpdater.java
@@ -60,9 +60,17 @@ public interface NeighborUpdater {
             state.handleNeighborChanged(level, pos, neighborBlock, orientation, movedByPiston);
             // Spigot start
         } catch (StackOverflowError ex) {
+            // Horizon start - fix update suppression crash
+            if (dev.gideonwhite1029.horizon.HorizonConfig.updateSuppressionCrashFix) {
+                throw new dev.gideonwhite1029.horizon.util.UpdateSuppressionException(pos, neighborBlock);
+            }
             level.lastPhysicsProblem = pos.immutable();
             // Spigot end
         } catch (Throwable var9) {
+            if (dev.gideonwhite1029.horizon.HorizonConfig.updateSuppressionCrashFix) {
+                throw new dev.gideonwhite1029.horizon.util.UpdateSuppressionException(pos, neighborBlock);
+            }
+            // Horizon end - fix update suppression crash
             CrashReport crashReport = CrashReport.forThrowable(var9, "Exception while updating neighbours");
             CrashReportCategory crashReportCategory = crashReport.addCategory("Block being updated");
             crashReportCategory.setDetail(
