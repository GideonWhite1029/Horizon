--- a/purpur-server/build.gradle.kts
+++ b/purpur-server/build.gradle.kts
@@ -13,7 +_,10 @@
     id("io.papermc.fill.gradle") version "1.0.11"
 }
 
+version = providers.gradleProperty("mcVersion").get() // Horizon - set version from mcVersion
+
 val paperMavenPublicUrl = "https://repo.papermc.io/repository/maven-public/"
+val purpurMavenPublicUrl = "https://repo.purpurmc.org/snapshots/" // Horizon - Initial build
 
 dependencies {
     mache("io.papermc:mache:26.1.2+build.2")
@@ -33,7 +_,29 @@
             outputDir = rootDirectory.dir("paper-server")
         }
     }
-    activeFork = purpur
+    // Horizon start - Initial build
+    val horizon = forks.register("horizon") {
+        forks = purpur
+        upstream.patchRepo("paperServer") {
+            upstreamRepo = purpur.patchedRepo("paperServer")
+            patchesDir = rootDirectory.dir("horizon-server/paper-patches")
+            outputDir = rootDirectory.dir("paper-server")
+        }
+        upstream.patchDir("purpurServer") {
+            upstreamPath = "purpur-server"
+            excludes = setOf(
+                "src/minecraft",
+                "paper-patches",
+                "minecraft-patches",
+                "build.gradle.kts",
+                "build.gradle.kts.patch"
+            )
+            patchesDir = rootDirectory.dir("horizon-server/purpur-patches")
+            outputDir = rootDirectory.dir("purpur-server")
+        }
+    }
+    activeFork = horizon
+    // Horizon end - Initial build
     // Purpur end - Rebrand
 
     /*updatingMinecraft { // Purpur start - TODO - snapshot - we don't need this
@@ -45,6 +_,7 @@
     libraryRepositories.addAll(
         "https://repo.maven.apache.org/maven2/",
         paperMavenPublicUrl,
+        purpurMavenPublicUrl, // Horizon - Initial build
     )
 }
 
@@ -105,10 +_,14 @@
     main {
         java { srcDir("../paper-server/src/main/java") }
         resources { srcDir("../paper-server/src/main/resources") }
+        java { srcDir("../purpur-server/src/main/java") } // Horizon - Initial build
+        resources { srcDir("../purpur-server/src/main/resources") } // Horizon - Initial build
     }
     test {
         java { srcDir("../paper-server/src/test/java") }
         resources { srcDir("../paper-server/src/test/resources") }
+        java { srcDir("../purpur-server/src/test/java") } // Horizon - Initial build
+        resources { srcDir("../purpur-server/src/test/resources") } // Horizon - Initial build
     }
 }
 val log4jPlugins = sourceSets.create("log4jPlugins") {
@@ -137,7 +_,12 @@
 }
 
 dependencies {
-    implementation(project(":purpur-api")) // Purpur
+    implementation(project(":horizon-api")) // Purpur // Horizon
+    // Horizon start
+    implementation("com.github.luben:zstd-jni:1.5.7-7")
+    implementation("at.yawk.lz4:lz4-java:1.11.0")
+    implementation("net.openhft:zero-allocation-hashing:2026.0")
+    // Horizon end
     implementation("ca.spottedleaf:concurrentutil:0.0.10")
     implementation("org.jline:jline-terminal-ffm:3.27.1") // use ffm on java 22+
     implementation("org.jline:jline-terminal-jni:3.27.1") // fall back to jni on java 21
@@ -191,6 +_,14 @@
     implementation("me.lucko:spark-paper:1.10.152")
 }
 
+tasks.withType<JavaCompile> {
+    val compilerArgs = options.compilerArgs
+    compilerArgs.add("-Xlint:-module")
+    compilerArgs.add("-Xlint:-removal")
+    compilerArgs.add("-Xlint:-dep-ann")
+    compilerArgs.add("--add-modules=jdk.incubator.vector")
+}
+
 tasks.jar {
     manifest {
         val git = Git(rootProject.layout.projectDirectory.path)
@@ -203,14 +_,14 @@
         val gitBranch = git.exec(providers, "rev-parse", "--abbrev-ref", "HEAD").get().trim()
         attributes(
             "Main-Class" to "org.bukkit.craftbukkit.Main",
-            "Implementation-Title" to "Purpur", // Purpur
+            "Implementation-Title" to "Horizon", // Purpur // Horizon
             "Implementation-Version" to implementationVersion,
             "Implementation-Vendor" to date,
-            "Specification-Title" to "Purpur", // Purpur
-            "Specification-Version" to project.version,
-            "Specification-Vendor" to "Purpur Team", // Purpur
-            "Brand-Id" to "purpurmc:purpur", // Purpur
-            "Brand-Name" to "Purpur", // Purpur
+            "Specification-Title" to "Horizon", // Purpur // Horizon
+            "Specification-Version" to mcVersion,
+            "Specification-Vendor" to "GideonWhite1029", // Purpur // Horizon
+            "Brand-Id" to "gideonwhite1029:horizon", // Purpur // Horizon
+            "Brand-Name" to "Horizon", // Purpur // Horizon
             "Build-Number" to (build ?: ""),
             "Build-Time" to buildTime.toString(),
             "Git-Branch" to gitBranch,
